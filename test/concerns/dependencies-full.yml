#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      persistence:
        size: 2Gi
        storageClass: longhorn

postgres:
  enabled: true  
  values:
    primary:
      persistence:
        size: 2Gi	
        storageClass: longhorn

rabbitmq:
  enabled: true  
  values:
    persistence:
      size: 2Gi
      storageClass: longhorn

elasticsearch:
  enabled: true  
  values:
    master:
      persistence:
        size: 2Gi
        storageClass: longhorn
    data:
      persistence:
        size: 2Gi
        storageClass: longhorn

redis:
  enabled: true

nats:
  enabled: true

etcd:
  enabled: true
