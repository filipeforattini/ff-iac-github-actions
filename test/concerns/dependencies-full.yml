#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      persistence:
        size: 2Gi	

postgres:
  enabled: true  
  values:
    primary:
      persistence:
        size: 2Gi	

rabbitmq:
  enabled: true  
  values:
    persistence:
      size: 2Gi

elasticsearch:
  enabled: true  
  values:
    master:
      persistence:
        size: 2Gi
    data:
      persistence:
        size: 2Gi

redis:
  enabled: true
