#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      nodeSelector:
        - name: arch
          value: amd64
      persistence:
        size: 2Gi
        storageClass: longhorn

postgres:
  enabled: true  
  values:
    primary:
      nodeSelector:
        - name: arch
          value: amd64
      persistence:
        size: 2Gi	
        storageClass: longhorn

rabbitmq:
  enabled: true 
  values:
    nodeSelector:
      - name: arch
        value: amd64
    persistence:
      size: 2Gi
      storageClass: longhorn

elasticsearch:
  enabled: true  
  values:
    master:
      nodeSelector:
        - name: arch
          value: amd64
      persistence:
        size: 2Gi
        storageClass: longhorn
    data:
      nodeSelector:
        - name: arch
          value: amd64
      persistence:
        size: 2Gi
        storageClass: longhorn

redis:
  enabled: true
  values:
    master:
      nodeSelector:
        - name: arch
          value: amd64
      persistence:
        enabled: false

nats:
  enabled: true
  values:
    nodeSelector:
      - name: arch
        value: amd64

etcd:
  enabled: true
  values:
    nodeSelector:
      - name: arch
        value: amd64