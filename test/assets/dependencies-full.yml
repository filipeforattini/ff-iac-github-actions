#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      persistence:
        enabled: false
        size: 2Gi
        storageClass: longhorn

postgres:
  enabled: true  
  values:
    primary:
      persistence:
        enabled: false
        size: 2Gi	
        storageClass: longhorn

rabbitmq:
  enabled: true 
  values:
    persistence:
      enabled: false
      size: 2Gi
      storageClass: longhorn

elasticsearch:
  enabled: true  
  values:
    master:
      persistence:
        size: 2Gi
        storageClass: longhorn
    data:
      persistence:
        enabled: false
        size: 2Gi
        storageClass: longhorn

redis:
  enabled: true
  values:
    master:
      persistence:
        enabled: false

nats:
  enabled: true
  values:
    nodeSelector:
      - name: arch
        value: amd64

etcd:
  enabled: true
  values:
    persistence:
      enabled: false